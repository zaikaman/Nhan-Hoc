# Biến môi trường cho backend
# Đổi tên file này thành .env và điền các giá trị thực

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Flask Environment
FLASK_ENV=production
FLASK_APP=base.py

# Port (Heroku sẽ tự động set, không cần thay đổi)
# PORT=5000
